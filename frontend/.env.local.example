# URL de l'API FastAPI pour le dev local (proxy Next.js → FastAPI)
# En production Railway, laisser vide — les routes /api/* proxy en interne.
DEEPSLEEP_INFERENCE_URL=http://127.0.0.1:8000

# Optionnel : appeler l'API directement (sans proxy Next.js)
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
